y,.ag-perm .copy{flex:1}.ag-scope b,.ag-perm b{display:block;color:#344258;font-size:10px}.ag-scope small,.ag-perm small{color:#8491a2;font-size:8px}.ag-child{height:25px;border:1px solid #dfe6ef;border-radius:6px;background:#fff;color:#66758a;font-size:8px}.ag-child.on{border-color:#b7d4f8;background:#f4f9ff;color:#1760b7}.ag-summary{display:grid;grid-template-columns:135px 1fr;border:1px solid #e1e7ef;border-radius:8px;overflow:hidden}.ag-summary dt,.ag-summary dd{margin:0;padding:9px 10px;border-bottom:1px solid #edf1f5;font-size:10px}.ag-summary dt{background:#fafbfd;color:#7d8999}.ag-summary dd{color:#3b4b61}.ag-summary dt:last-of-type,.ag-summary dd:last-of-type{border:0}.ag-confirm{display:flex;align-items:flex-start;gap:8px;margin-top:10px;padding:8px 9px;border:1px solid #e1e7ef;border-radius:8px;background:#fafbfd;font-size:9px}.ag-drawer{position:absolute;right:0;top:0;bottom:0;width:min(510px,92vw);display:flex;flex-direction:column;background:#fff;border-left:1px solid #dfe6ef;box-shadow:-18px 0 50px rgba(18,37,63,.18);pointer-events:auto}.ag-drawer .ag-mb{flex:1}.ag-hist{padding:9px 10px;margin-top:7px;border:1px solid #e3e8ef;border-radius:7px}.ag-hist b{font-size:9px;color:#3b4b61}.ag-hist p{margin:3px 0;color:#7a8798;font-size:8px}@media(max-width:1200px){.ag-filter{flex-wrap:wrap}.ag-stats{grid-template-columns:repeat(2,1fr)}.ag-table{min-width:1050px}.ag-panel{overflow:auto}}
`;document.head.appendChild(s);}
function filtered(){let q=S.f.q.toLowerCase();return S.admins.filter(a=>{let u=m(a),text=[u.name,u.no,u.dept,u.job,sc(a).join(' '),a.perm.map(pn).join(' ')].join(' ').toLowerCase();return(!q||text.includes(q))&&(S.f.type==='all'||a.type===S.f.type)&&(S.f.status==='all'||a.status===S.f.status)&&(S.f.scope==='all'||a.type==='SYSTEM'||a.scope.some(x=>x.id===S.f.scope))&&(S.f.perm==='all'||a.perm.includes(S.f.perm))})}
function sel(k,opts){let v=opts.find(x=>x[0]===S.f[k])?.[1]||opts[0][1];return`<div class="ag-sel"><button onclick="AdminGrant.toggleDrop('${k}',event)">${e(v)}⌄</button>${S.drop===k?`<div class="ag-pop" onclick="event.stopPropagation()">${opts.map(x=>`<button class="${S.f[k]===x[0]?'on':''}" onclick="AdminGrant.setFilter('${k}','${x[0]}')">${e(x[1])}</button>`).join('')}</div>`:''}</div>`}
function tags(arr,neutral=''){return`<div class="ag-tags">${arr.slice(0,3).map(x=>`<span class="ag-tag ${neutral}">${e(x)}</span>`).join('')}${arr.length>3?`<span class="ag-tag neutral">+${arr.length-3}</span>`:''}</div>`}
function row(a){let u=m(a),ps=a.type==='SYSTEM'?['全部管理中心能力']:a.perm.map(pn);return`<tr><td><div class="ag-person"><span class="ag-av">${u.initial}</span><span><b>${u.name}</b><small>${u.no} · ${u.job}</small></span></div></td><td><span class="ag-badge ${a.type==='COMPANY'?'sub':''}">${tt(a.type)}</span></td><td>${tags(sc(a))}</td><td>${tags(ps,'neutral')}${risk(a)?'<span class="ag-risk">! 高风险权限</span>':''}</td><td><span class="ag-status ${a.status==='disabled'?'off':''}"><i></i>${st(a.status)}</span></td><td><span class="ag-update">${a.time}<small>${a.by} 更新</small></span></td><td><div class="ag-actions"><button class="ag-btn text" onclick="AdminGrant.edit('${a.id}')">编辑</button><button class="ag-btn icon" aria-label="更多操作" onclick="AdminGrant.more('${a.id}',event)">${I('more')}</button>${S.menu===a.id?menu(a):''}</div></td></tr>`}
function menu(a){return`<div class="ag-menu" onclick="event.stopPropagation()"><button onclick="AdminGrant.details('${a.id}')">查看详情</button><button onclick="AdminGrant.status('${a.id}','${a.status==='active'?'disabled':'active'}')">${a.status==='active'?'停用授权':'启用授权'}</button><button onclick="AdminGrant.transfer('${a.id}')">移交管理员授权</button><button class="danger" onclick="AdminGrant.del('${a.id}')">删除管理员授权</button></div>`}
function page(){let x=filtered(),active=S.admins.filter(a=>a.status==='active').length,covered=new Set(S.admins.filter(a=>a.type==='COMPANY'&&a.status==='active').flatMap(a=>a.scope.map(s=>s.id))).size,rr=S.admins.filter(a=>a.status==='active'&&risk(a)).length;return`<div id="ag"><div class="ag-head"><div><h3>管理员设置</h3><p>配置管理中心入口、组织范围和菜单权限；权限变更后立即生效。</p></div><div class="right"><span class="ag-tag">当前范围：贵安发展集团及全部下级组织</span><button class="ag-btn primary" onclick="AdminGrant.create()">${I('plus')}添加管理员</button></div></div><div class="ag-note">${I('shield')}<div><b>管理员授权与业务身份相互独立</b><br><span>停用或删除授权不会删除成员，也不会自动改变部门负责人、文件管理员或个人空间文件归属。</span></div><button onclick="AdminGrant.guide()">查看授权边界</button></div><div class="ag-stats"><div class="ag-stat"><span>管理员总数</span><strong>${S.admins.length}</strong></div><div class="ag-stat"><span>当前生效授权</span><strong>${active}</strong></div><div class="ag-stat"><span>已覆盖公司</span><strong>${covered}</strong></div><div class="ag-stat"><span>高风险权限账号</span><strong>${rr}</strong></div></div><div class="ag-panel"><div class="ag-filter"><label class="ag-search">${I('search')}<input aria-label="搜索管理员" value="${e(S.f.q)}" placeholder="搜索姓名、工号、公司、部门或权限" oninput="AdminGrant.query(this.value)"></label>${sel('type',[['all','全部类型'],['SYSTEM','系统管理员'],['COMPANY','分级管理员']])}${sel('status',[['all','全部状态'],['active','生效中'],['disabled','已停用']])}${sel('scope',[['all','全部管辖范围'],...O.filter(o=>!o.deny).map(o=>[o.id,o.n])])}${sel('perm',[['all','全部权限能力'],...P.map(p=>[p.k,p.n])])}<button class="ag-reset" onclick="AdminGrant.reset()">${I('reset')}重置</button><span class="ag-count">共 ${x.length} 条结果</span></div>${S.loading?'<div class="ag-empty"><div><span class="ag-spin"></span><p>正在加载管理员授权…</p></div></div>':S.error?`<div class="ag-empty"><div><b>加载失败</b><p>${e(S.error)}</p><button class="ag-btn" onclick="AdminGrant.retry()">重新加载</button></div></div>`:x.length?`<table class="ag-table"><colgroup><col style="width:18%"><col style="width:10%"><col style="width:18%"><col style="width:22%"><col style="width:9%"><col style="width:13%"><col style="width:10%"></colgroup><thead><tr><th>管理员</th><th>管理员类型</th><th>管辖范围</th><th>授权能力</th><th>状态</th><th>最近更新</th><th>操作</th></tr></thead><tbody>${x.map(row).join('')}</tbody></table>`:'<div class="ag-empty"><div><b>没有找到匹配的管理员授权</b><p>请调整筛选条件。</p><button class="ag-btn" onclick="AdminGrant.reset()">重置筛选</button></div></div>'}</div></div>`}
function ov(){let r=document.getElementById('ag-ov');if(!r){r=document.createElement('div');r.id='ag-ov';document.body.appendChild(r)}if(!S.overlay){r.innerHTML='';return}let t=S.overlay.type;r.innerHTML=t==='wizard'?wizard():t==='details'?detail():t==='status'?statusModal():t==='delete'?deleteModal():t==='transfer'?transferModal():guideModal()}
function frame(title,sub,body,foot,drawer=0){return drawer?`<div class="ag-layer" onclick="if(event.target===this)AdminGrant.close()"><aside class="ag-drawer" role="dialog" aria-modal="true" aria-label="${e(title)}"><header class="ag-mh"><div><h4>${e(title)}</h4><p>${e(sub)}</p></div><button class="x" onclick="AdminGrant.close()">×</button></header><div class="ag-mb">${body}</div><footer class="ag-mf">${foot}</footer></aside></div>`:`<div class="ag-layer" onclick="if(event.target===this)AdminGrant.close()"><section class="ag-modal" role="dialog" aria-modal="true" aria-label="${e(title)}"><header class="ag-mh"><div><h4>${e(title)}</h4><p>${e(sub)}</p></div><button class="x" aria-label="关闭" onclick="AdminGrant.close()">×</button></header><div class="ag-mb">${body}</div><footer class="ag-mf">${foot}</footer></section></div>`}
function steps(n){return`<div class="ag-steps">${['选择成员','类型与范围','权限能力','确认授权'].map((x,i)=>`<div class="ag-step ${i+1===n?'on':i+1<n?'done':''}"><b>${i+1<n?'✓':i+1}</b>${x}</div>`).join('')}</div>`}
function w1(){let w=S.w;if(w.mode==='edit'){let u=M.find(x=>x.id===w.uid);return`${steps(1)}<div class="ag-member on"><span class="ag-av">${u.initial}</span><span class="copy"><b>${u.name} · ${u.no}</b><small>${u.dept} · ${u.job}</small></span><span class="ag-tag neutral">成员信息只读</span></div><p class="ag-help">更换接收人请使用管理员移交，避免把编辑授权误当成移交。</p>`}let q=w.mq.toLowerCase(),list=M.filter(u=>!q||[u.name,u.no,u.dept,u.job].join(' ').toLowerCase().in