cludes(q));return`${steps(1)}<div class="ag-field"><label>选择成员 *</label><input class="ag-input" value="${e(w.mq)}" placeholder="搜索姓名、工号、手机号或邮箱" oninput="AdminGrant.wf('mq',this.value)"><p class="ag-help">已停用成员和已拥有相同授权的成员不可重复添加。</p></div><div class="ag-list">${list.map(u=>{let dis=u.status!=='active'||S.admins.some(a=>a.uid===u.id&&a.type===w.type&&a.status==='active');return`<button class="ag-member ${w.uid===u.id?'on':''}" ${dis?'disabled':''} onclick="AdminGrant.pickMember('${u.id}')"><span class="ag-radio ${w.uid===u.id?'on':''}"></span><span class="ag-av">${u.initial}</span><span class="copy"><b>${u.name} · ${u.no}</b><small>${u.dept} · ${u.job}</small></span><span class="ag-tag neutral">${u.status!=='active'?'账号已停用':dis?'已有相同授权':'账号正常'}</span></button>`}).join('')}</div>${err()}`}
function w2(){let w=S.w;return`${steps(2)}<div class="ag-typegrid"><button class="ag-type ${w.type==='SYSTEM'?'on':''}" onclick="AdminGrant.pickType('SYSTEM')"><b>系统管理员</b><p>通常管理全集团，可进入全部或大部分管理中心模块，属于高权限账号。</p></button><button class="ag-type ${w.type==='COMPANY'?'on':''}" onclick="AdminGrant.pickType('COMPANY')"><b>分级管理员</b><p>只能管理被授权公司或组织范围，不能越权访问其他组织。</p></button></div>${w.type==='SYSTEM'?'<div class="ag-danger" style="margin-top:10px">系统管理员范围为全集团。切换为系统管理员需要高风险确认。</div>':`<div class="ag-field" style="margin-top:11px"><label>管辖范围 *</label>${O.map(o=>{let x=w.scope.find(s=>s.id===o.id);return`<div class="ag-scope ${o.deny?'deny':''}"><button class="ag-check ${x?'on':''}" ${o.deny?'disabled':''} onclick="AdminGrant.scope('${o.id}')">${x?'✓':''}</button><span class="copy"><b>${o.n}</b><small>${o.deny?'超出当前操作者可授权范围':'公司节点，可选择是否包含下级组织'}</small></span>${x?`<button class="ag-child ${x.child?'on':''}" onclick="AdminGrant.child('${o.id}')">${x.child?'包含下级组织':'仅当前公司'}</button>`:''}</div>`}).join('')}</div>`}${err()}`}
function w3(){let w=S.w;return`${steps(3)}<div class="ag-warn">管理范围和菜单权限是两个不同维度；分级管理员只能获得当前操作者拥有的权限。</div>${P.map(p=>{let on=w.perm.includes(p.k),deny=!S.operator.system&&!S.operator.perm.includes(p.k);return`<div class="ag-perm ${deny?'deny':''}" onclick="${deny?'':`AdminGrant.perm('${p.k}')`}"><button class="ag-check ${on?'on':''}" ${deny?'disabled':''}>${on?'✓':''}</button><span class="copy"><b>${p.n}${p.r?' · 高风险':''}</b><small>${p.d}${deny?'；当前操作者无权授予':''}</small></span></div>`}).join('')}${err()}`}
function w4(){let w=S.w,u=M.find(x=>x.id===w.uid),ss=w.type==='SYSTEM'?['全集团']:w.scope.map(x=>on(x.id)+(x.child?'（含下级）':'')),pp=w.type==='SYSTEM'?['全部管理中心能力']:w.perm.map(pn);return`${steps(4)}<dl class="ag-summary"><dt>成员信息</dt><dd>${u?.name||''} · ${u?.no||''}<br>${u?.dept||''} · ${u?.job||''}</dd><dt>管理员类型</dt><dd>${tt(w.type)}</dd><dt>管辖范围</dt><dd>${tags(ss)}</dd><dt>权限能力</dt><dd>${tags(pp,'neutral')}</dd><dt>状态</dt><dd>${w.status==='active'?'立即启用并刷新会话':'保存为停用状态'}</dd><dt>备注</dt><dd>${e(w.note||'未填写')}</dd></dl><div class="ag-field" style="margin-top:10px"><label>授权备注</label><textarea class="ag-textarea" oninput="AdminGrant.wf('note',this.value)">${e(w.note)}</textarea></div><label class="ag-confirm"><button class="ag-check ${w.ok?'on':''}" onclick="AdminGrant.confirm()">${w.ok?'✓':''}</button><span>我已确认：本操作只配置管理员授权，不自动改变部门负责人、文件管理员、成员部门归属、个人空间文件或文件夹所有权。</span></label>${risk({type:w.type,perm:w.perm})?'<div class="ag-danger" style="margin-top:9px">本次包含高风险权限，提交后立即生效并记录审计日志。</div>':''}${err()}`}
function err(){return S.w?.err?`<p class="ag-error">${e(S.w.err)}</p>`:''}
function wizard(){let w=S.w,body=[w1,w2,w3,w4][w.step-1](),u=w.mode==='edit'?m(S.admins.find(a=>a.id===w.id)):null,back=w.step>1?'<button class="ag-btn" onclick="AdminGrant.prev()">上一步</button>':'',next=w.step<4?'<button class="ag-btn primary" onclick="AdminGrant.next()">下一步</button>':`<button class="ag-btn primary" ${w.busy?'disabled':''} onclick="AdminGrant.save()">${w.busy?'正在保存…':w.mode==='edit'?'保存修改':'确认授权'}</button>`;return frame(w.mode==='edit'?`编辑管理员 - ${u.name}`:'添加管理员',w.mode==='edit'?'成员信息只读；类型、范围、权限、状态和备注可调整。':'四步完成成员、类型与范围、权限能力和授权确认。',body,`<button class="ag-btn" onclick="AdminGrant.close()">取消</button>${back}${next}`)}
function detail(){let a=S.admins.find(x=>x.id===S.overlay.id),u=m(a),ps=a.type==='SYSTEM'?P:a.perm.map(k=>P.find(p=>p.k===k));let b=`<div class="ag-member on"><span class="ag-av">${u.initial}</span><span class="copy"><b>${u.name} · ${u.no}</b><small>${u.dept} · ${u.job}</small></span></div><dl class="ag-summary" style="margin-top:10px"><dt>类型</dt><dd>${tt(a.type)}</dd><dt>状态</dt><dd>${st(a.status)}</dd><dt>范围</dt><dd>${sc(a).join('、')}</dd><dt>备注</dt><dd>${e(a.note)}</dd><dt>最近更新</dt><dd>${a.time} · ${a.by}</dd></dl><h5>权限能力</h5>${ps.map(p=>`<div class="ag-perm"><span class="ag-check on">✓</span><span class="copy"><b>${p.n}${p.r?' · 高风险':''}</b><small>${p.d}</small></span></div>`).join('')}<h5>权限变更记录</h5>${a.history.map((h,i)=>`<div class="ag-hist"><b>${e(h)}</b><p>${i? '历史授权记录':'最近变更 · '+a.time+' · '+a.by}</p></div>`).join('')}`;return frame('管理员详情',`${u.name} · ${tt(a.type)}`,b,`<button class="ag-btn" onclick="AdminGrant.close()">关闭</button><button class="ag-btn primary" onclick="AdminGrant.edit('${a.id}')">编辑授权</button>`,1)}
function statusModal(){let a=S.admins.find(x=>x.id===S.overlay.id),u=m(a),en=S.overlay.to==='active';return frame(en?'启用管理员授权':'停用管理员授权',en?'恢复已保存的类型、范围和权限能力。':'优先使用停用处理临时停权和岗位调整。',`<div class="${en?'ag-warn':'ag-danger'}">${en?'启用后立即刷新成员会话。':'停用后成员失去对应管理中心权限，但普通成员账号仍然保留。'}</div><dl class="ag-summary" style="margin-top:10px"><dt>管理员</dt><dd>${u.name} · ${u.no}</dd><dt>类型</dt><dd>${tt(a.type)}</dd><dt>范围</dt><dd>${sc(a).join('、')}</dd><dt>权限</dt><dd>${a.type==='SYSTEM'?'全部管理中心能力':a.perm.map(pn).join('、')}</dd></dl><div class="ag-warn" style="margin-top:10px">不影响所属部门、部门负责人、文件管理员、个人空间文件或文件夹所有权；保留历史授权记录。</div>`,`<button class="ag-btn" onclick="AdminGrant.close()">取消</button><button class="ag-btn ${en?'primary':'danger'}" onclick="AdminGrant.applyStatus()">确认${en?'启用':'停用'}</button>`)}
function deleteModal(){let a=S.admins.find(x=>x.id===S.overlay.id),u=m(a);return frame('删除管理员授权','高风险操作：优先考虑停用或移交。',`<div class="ag-danger">只删除管理员授权，不删除成员账号；保留必要审计记录，不处理其他身份和文件资产。</div><dl class="ag-summary" style="margin-top:10px"><dt>管理员</dt><dd>${u.name} · ${u.no}</dd><dt>类型</dt><dd>${tt(a.type)}</dd><dt>范围</dt><dd>${sc(a).join('、')}</dd></dl><div class="ag-field" style="margin-top:10px"><label>输入姓名“${u.name}”确认删除</label><input id="ag-del-name" class="ag-input"><p id="ag-del-err" class="ag-error" style="display:none">姓名不一致。</p></div>`,`<button class="ag-btn" onclick="AdminGrant.close()">取消</button><button class="ag-btn" onclick="AdminGrant.status('${a.id}','disabled')">改为停用</button><button class="ag-btn danger" onclick="AdminGrant.applyDelete()">删除授权</button>`)}
function transferModal(){let a=S.admins.find(x=>x.id===S.overlay.id),u=m(a),t=S.overlay,rec=M.filter(x=>x.id!==u.id&&x.status==='active');return frame('移交管理员授权',`原管理员：${u.name} · ${tt(a.type)}`,`<div class="ag-warn">只移交管理员类型、管辖范围、权限能力和必要授权记录，不处理部门负责人、文件管理员、部门归属、个人空间文件或文件夹所有权。</div><div class="ag-field" style="margin-top:10px"><label>接收管理员 *</label><div class="ag-list">${rec.map(x=>`<button class="ag-member ${t.to===x.id?'on':''}" onclick="AdminGrant.pickRecipient('${x.id}')"><span class="ag-radio ${t.to===x.id?'on':''}"></span><span class="ag-av">${x.initial}</span><span class="copy"><b>${x.name} · ${x.no}</b><small>${x.dept} · ${x.job}</small></span></button>`).join('')}</div></div><dl class="ag-summary"><dt>授权类型</dt><dd>${tt(a.type)}</dd><dt>管辖范围</dt><dd>${sc(a).join('、')}</dd><dt>权限能力</dt><dd>${a.type==='SYSTEM'?'全部管理中心能力':a.perm.map(pn).join('、')}</dd><dt>原授权处理</dt><dd><button class="ag-child ${t.stop?'on':''}" onclick="AdminGrant.transferStop()">${t.stop?'移交后停用原授权':'保留原授权'}</button></dd></dl><label class="ag-confirm"><button class="ag-check ${t.ok?'on':''}" onclick="AdminGrant.transferOk()">${t.ok?'✓':''}</button><span>我已核对接收人账号正常、授权范围不会越权，且理解本移交不处理其他身份和文件资产。</span></label>${t.err?`<p class="ag-error">${e(t.err)}</p>`:''}`,`<button class="ag-btn" onclick="AdminGrant.close()">取消</button><button class=