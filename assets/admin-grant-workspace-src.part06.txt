"ag-btn primary" onclick="AdminGrant.applyTransfer()">确认移交</button>`)}
function guideModal(){return frame('管理员授权边界','管理范围与菜单权限必须分别配置和校验。',`<div class="ag-warn"><b>本模块处理：</b>系统管理员/分级管理员类型、授权组织范围、管理中心菜单权限、状态、备注和变更记录。</div><div class="ag-danger" style="margin-top:9px"><b>本模块不自动处理：</b>成员账号、部门负责人、文件管理员、成员部门归属、个人空间文件、文件夹所有权；成员部门变化也不会自动扩大管理员权限。</div>`,`<button class="ag-btn primary" onclick="AdminGrant.close()">我知道了</button>`)}
function refresh(){S.drop='';S.menu='';let x=document.querySelector('.admin-content');if(x&&window.state?.page==='admin'&&window.state?.adminTab==='grant')x.innerHTML=page();else window.render?.();ov()}
function close(){S.overlay=null;S.w=null;S.menu='';S.drop='';ov();let x=document.querySelector('.admin-content');if(x&&window.state?.adminTab==='grant')x.innerHTML=page()}
function toast(x,t='success'){window.toast?window.toast(x,t):console.info(x)}
function validate(){let w=S.w;w.err='';if(w.step===1&&!w.uid)w.err='请选择账号正常的成员。';if(w.step===2&&w.type==='COMPANY'&&!w.scope.length)w.err='分级管理员必须选择管辖范围。';if(w.step===2&&w.type==='SYSTEM'&&!S.operator.system)w.err='当前操作者无权授予系统管理员。';if(w.step===3&&w.type==='COMPANY'&&!w.perm.length)w.err='请至少选择一项权限能力。';if(w.step===4&&!w.ok)w.err='请确认授权边界和影响。';ov();return!w.err}
function install(){css();if(typeof window.adminContent!=='function'||!window.state)return 0;if(window.adminContent===wrapped)return 1;wrapped.base=window.adminContent;window.adminContent=wrapped;if(state.page==='admin'&&state.adminTab==='grant')window.render?.();return 1}function wrapped(){return state?.adminTab==='grant'?page():wrapped.base?.()||''}
window.AdminGrant={state:S,install,query(v){S.f.q=v;document.querySelector('.admin-content').innerHTML=page()},toggleDrop(k,ev){ev?.stopPropagation();S.menu='';S.drop=S.drop===k?'':k;document.querySelector('.admin-content').innerHTML=page()},setFilter(k,v){S.f[k]=v;refresh()},reset(){S.f={q:'',type:'all',status:'all',scope:'all',perm:'all'};refresh()},retry(){S.error='';S.loading=1;refresh();setTimeout(()=>{S.loading=0;refresh();toast('管理员授权已重新加载')},350)},more(id,ev){ev?.stopPropagation();S.drop='';S.menu=S.menu===id?'':id;document.querySelector('.admin-content').innerHTML=page()},guide(){S.overlay={type:'guide'};ov()},create(){S.w={mode:'create',step:1,uid:'',mq:'',type:'COMPANY',scope:[],perm:['dept','member','memberContent','report','log'],status:'active',note:'',ok:0,err:'',busy:0};S.overlay={type:'wizard'};ov()},edit(id){let a=S.admins.find(x=>x.id===id);S.w={mode:'edit',id,step:1,uid:a.uid,mq:'',type:a.type,scope:JSON.parse(JSON.stringify(a.scope)),perm:[...a.perm],status:a.status,note:a.note,ok:0,err:'',busy:0};S.overlay={type:'wizard'};ov()},details(id){S.menu='';S.overlay={type:'details',id};ov()},close,wf(k,v){S.w[k]=v;S.w.err='';ov()},pickMember(id){S.w.uid=id;S.w.err='';ov()},pickType(t){S.w.type=t;S.w.err='';if(t==='SYSTEM'){S.w.scope=[];S.w.perm=[...all]}else S.w.perm=S.w.perm.filter(k=>!['removeDept','removeMember','grantSubAdmin'].includes(k));ov()},scope(id){if(!S.operator.scopes.includes(id)){S.w.err='不能选择当前操作者无权管理的组织。';return ov()}let x=S.w.scope.find(s=>s.id===id);S.w.scope=x?S.w.scope.filter(s=>s.id!==id):[...S.w.scope,{id,child:1}];S.w.err='';ov()},child(id){let x=S.w.scope.find(s=>s.id===id);if(x)x.child=!x.child;ov()},perm(k){if(!S.operator.system&&!S.operator.perm.includes(k)){S.w.err='不能授予当前操作者自己没有的权限。';return ov()}S.w.perm=S.w.perm.includes(k)?S.w.perm.filter(x=>x!==k):[...S.w.perm,k];S.w.err='';ov()},next(){if(validate()){S.w.step++;S.w.err='';ov()}},prev(){S.w.step=Math.max(1,S.w.step-1);S.w.err='';ov()},confirm(){S.w.ok=!S.w.ok;S.w.err='';ov()},save(){if(!validate())return;let w=S.w,u=M.find(x=>x.id===w.uid);w.busy=1;ov();setTimeout(()=>{let d={uid:w.uid,type:w.type,scope:w.type==='SYSTEM'?[]:JSON.parse(JSON.stringify(w.scope)),perm:w.type==='SYSTEM'?[...all]:[...w.perm],status:w.status,time:now(),by:'张明远',note:w.note||'未填写'};if(w.mode==='edit'){let i=S.admins.findIndex(x=>x.id===w.id),a=S.admins[i];S.admins[i]={...a,...d,history:['编辑类型、范围、权限或状态，已立即生效',...a.history]};toast(`已保存 ${u.name} 的管理员授权`)}else{S.admins.unshift({id:'a'+Date.now(),...d,history:['创建管理员授权并记录审计日志']});toast(`已为 ${u.name} 添加${tt(w.type)}授权`)}close();refresh()},420)},status(id,to){S.menu='';S.overlay={type:'status',id,to};ov()},applyStatus(){let o=S.overlay,a=S.admins.find(x=>x.id===o.id),u=m(a);a.status=o.to;a.time=now();a.by='张明远';a.history.unshift(`${o.to==='active'?'启用':'停用'}管理员授权；成员账号及其他身份保留`);close();refresh();toast(`已${o.to==='active'?'启用':'停用'} ${u.name} 的管理员授权`)},del(id){S.menu='';S.overlay={type:'delete',id};ov()},applyDelete(){let a=S.admins.find(x=>x.id===S.overlay.id),u=m(a),v=document.getElementById('ag-del-name').value.trim();if(v!==u.name){document.getElementById('ag-del-err').style.display='block';return}S.admins=S.admins.filter(x=>x.id!==a.id);close();refresh();toast(`已删除 ${u.name} 的管理员授权，成员账号仍保留`,'warning')},transfer(id){S.menu='';S.overlay={type:'transfer',id,to:'',stop:1,ok:0,err:''};ov()},pickRecipient(id){S.overlay.to=id;S.overlay.err='';ov()},transferStop(){S.overlay.stop=!S.overlay.stop;ov()},transferOk(){S.overlay.ok=!S.overlay.ok;S.overlay.err='';ov()},applyTransfer(){let o=S.overlay,a=S.admins.find(x=>x.id===o.id),r=M.find(x=>x.id===o.to);if(!r)return o.err='请选择账号正常的接收管理员。',ov();if(!o.ok)return o.err='请确认管理员移交的授权内容和影响边界。',ov();let ex=S.admins.find(x=>x.uid===r.id&&x.type===a.type);if(ex){ex.scope=[...ex.scope,...a.scope.filter(s=>!ex.scope.some(t=>t.id===s.id))];ex.perm=[...new Set([...ex.perm,...a.perm])];ex.status='active';ex.time=now();ex.history.unshift(`接收自 ${m(a).name} 的管理员授权`)}else S.admins.unshift({...JSON.parse(JSON.stringify(a)),id:'a'+Date.now(),uid:r.id,status:'active',time:now(),by:'张明远',history:[`接收自 ${m(a).name} 的管理员授权`]});if(o.stop){a.status='disabled';a.time=now();a.history.unshift(`授权移交给 ${r.name} 后停用原授权`)}close();refresh();toast(`已将管理员授权移交给 ${r.name}`)}};
document.addEventListener('click',ev=>{if(!ev.target.closest('#ag .ag-sel')&&!ev.target.closest('#ag .ag-actions')&&(S.drop||S.menu)){S.drop='';S.menu='';let x=document.querySelector('.admin-content');if(x&&state?.adminTab==='grant')x.innerHTML=page()}});let boot=()=>{if(!install())setTimeout(boot,100)};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:1}):boot();addEventListener('load',()=>{setTimeout(install,180);setTimeout(install,800);setTimeout(install,1600)},{once:1});
})();
